scrape_configs:
  - job_name: ipmi
    static_configs:
{% for host in groups['prometheus-node-exporter'] %}
      - targets:
        - '{{ 'api' | kolla_address(host) | put_address_in_context('url') }}:9290'
{% endfor %}
