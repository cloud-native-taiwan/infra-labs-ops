# period_reconcile is a library/CLI consumed by other tools' images; it has no
# environment configuration of its own. All behaviour is passed on the command
# line by the consuming tool's crontab/entrypoint. This file exists only to
# satisfy the tools/ directory convention.
